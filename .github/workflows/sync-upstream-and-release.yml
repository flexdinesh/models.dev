name: Sync Upstream And Release

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

concurrency:
  group: sync-upstream-and-release
  cancel-in-progress: true

jobs:
  sync-upstream-and-release:
    uses: flexdinesh/models.dev/.github/workflows/sync-upstream-and-release.yml@web-fast-overlay
    secrets: inherit
